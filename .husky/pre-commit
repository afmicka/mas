set +e

# Node version manager support for GUI git clients (Sourcetree, VS Code, etc.)
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" && nvm use > /dev/null 2>&1

npx lint-staged || true
